/**
 * AddProductModal — thin re-export of ProductFormModal in Add mode.
 * Kept for backwards compatibility; Products.jsx now uses ProductFormModal directly.
 */
export { default } from './ProductFormModal';
